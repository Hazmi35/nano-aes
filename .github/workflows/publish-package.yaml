name: Release package to NPM

on:
  release:
    types: [created]

jobs:
  publish:
    strategy:
      matrix:
        include:
          - registry-url: https://registry.npmjs.org/
            token_secret_key: NPM_TOKEN
    uses: Hazmi35/workflows/.github/workflows/pnpm-publish-typescript.yaml@902d2875b7564a44937b905f54a4002b3429ea56 # master
    with:
      registry-url: ${{ matrix.registry-url }}
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets[matrix.token_secret_key] }}
